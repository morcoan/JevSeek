# Argument offload implementation gate

User explicitly authorized implementation after correcting the objective to paid Flash usage and total cost at preserved performance. This study calls the actual `Models.arguments` method, not a stand-in selection endpoint. No app release/build/push, VM or generated-code execution.

Two arms on each fresh authored case: original nonthinking Flash (`off`), new argument path (`bounded`, confidence-abstention floor0.90 fixed before calls).6development+16confirmation. Includes schema-unique calls, role-sensitive closed enums/booleans/optional parameters, verbatim read paths/ranges, unavailable computed read ranges, unsupported optional integers and unconstrained generation. Whole schema unsupported means no Jev tax; model FALLBACK/low confidence/errors invoke the original Flash input unchanged. Confidence is an abstention heuristic, NOT proof of correctness.

No candidate built from hidden expected answers: read candidates come only from user text/observed workspace metadata. MCP candidates come only from actual supplied enums/types. Expected argument dictionaries are for grading after both arms finish. JSON schema validation plus effective-argument comparison (declared defaults applied) is primary. Equivalent end=-1/file-last-line forms accepted for declared known file lengths. Read arguments additionally index owned fixture lines; no model commands/code execute. This is an argument-stage benchmark, not a complete coding-agent quality guarantee.

Use live pinned Jev1.13 and nonthinking deepseek-flash. Every provider attempt/usage/fallback retained. No result-based retries, no prompt tuning on confirmation.2workers,60sFlash timeout,10sJev timeout,0SDK retries in research,300sstart-call deadline per phase, hard70attempt cap across both phases(6*3+16*3=66maximum; singleton/unsupported skip calls). Stop after this sample.

Gate to confirmation: no paired correctness regressions and lower aggregate estimated off-peak API cost than original, all usage available, >=1accepted semantic bypass. Gate for default semantic enablement additionally requires confirmation0paired regressions, >=20%cost reduction, median endpoint time no worse,0unaccounted API charges, all scoped offline tests. Otherwise keep deterministic default; bounded remains explicit opt-in. Singleton code bypass is validated separately by exhaustive small-schema/offline checks.

Report actual Flash calls/input/output/$ saved and Jev costs ADDED, not combined tokens. Current verified public rates: Jev$.042/Minput/freeoutput; Flashoffpeak inputmiss.15/cachehit.003/output.60 perM(peakdouble). Estimates, not invoices; count Flash reasoning within completion only. Source/fixture hashes frozen before paid calls. No claiming unknown/defaulted fields are safe, no selective result deletion, no universal no-regression guarantee from22authored cases.
